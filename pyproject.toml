[project]
name = "causal-inference-basic-book"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
dev-dependencies = [
    "jupyter>=1.1.1",
    "ruff>=0.7.0",
]
